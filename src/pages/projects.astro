---
import Layout from "../layouts/content.astro";

const title = "Projects";
const description = "Notes on the process of building";
---

<Layout title={title} description={description} />

<style>
	@design-tokens url("../styles/generated-tokens.json") format("style-dictionary3");

	:root {
		--content-header-bg: dt("color.bg.projects");
		--content-header-text: dt("color.text.projects");
	}
</style>
